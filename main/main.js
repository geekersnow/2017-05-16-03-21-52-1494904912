module.exports = function main(email, suffixes) {
  // Write your code here
  var str = email.spilt("@");
  for (var i = 0; i< length.suffixes; i++)
	  if (str[1] == suffixes[i])
		return true;
	   else 
		return false;
};
